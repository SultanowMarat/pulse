# ═══════════════════════════════════════════════════════════════════════════════
# Конфиг: Redis (контейнер messenger-redis). Запуск: redis-server /path/to/redis.conf
# ═══════════════════════════════════════════════════════════════════════════════

dir /data
appendonly yes
appendfilename "appendonly.aof"
port 6379
bind 0.0.0.0
loglevel notice
